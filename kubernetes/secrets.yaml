apiVersion: v1
kind: Secret
metadata:
  name: spam-classifier-secrets
  namespace: spam-classifier
type: Opaque
data:
  # These are base64 encoded placeholder values
  # In production, these should come from Vault
  # Use: echo -n "value" | base64
  DOCKER_USERNAME: ZG9ja2VydXNlcg==  # dockeruser
  DOCKER_PASSWORD: ZG9ja2VycGFzcw==  # dockerpass
stringData:
  # Note: In production, remove this file and use Vault for secret management
  vault-enabled: "true"
